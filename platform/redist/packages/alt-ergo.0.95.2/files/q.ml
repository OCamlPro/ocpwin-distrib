type t = {num:int;den:int}
let one = {num=1;den=1}
let neg _ = one
let one = one
let gcd _ _ = one
let div _ _ = one
let mul _ _ = one
let num _ = one
let den _ = one
let make _ _ = one
let to_string _ = "one"
